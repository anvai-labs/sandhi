name: publish-crates (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          echo "::error::This unguarded publisher is retired. Use the safeguarded unified release workflow; see RELEASING.md."
          exit 1
