{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RunCostTreeV1",
  "description": "The cost tree of one agent run (ADR-0005 D7): every [`UsageEvent`] sharing a `run_id`, folded per step and assembled by `parent_id`. Neutral units only — no dollars.\n\nThis is the **definition** of the run fold (TD-0009 D6): the durable store filters rows by `run_id` and calls [`RunCostTreeV1::from_events`] — SQL never re-derives the arithmetic.",
  "type": "object",
  "required": [
    "roots",
    "run_id",
    "total"
  ],
  "properties": {
    "roots": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/RunUsageNodeV1"
      }
    },
    "run_id": {
      "type": "string"
    },
    "total": {
      "description": "The grand total over every event of the run (`key` = `run_id`). Always equals the sum of the root rollups — cycles and orphans are re-rooted, never dropped.",
      "allOf": [
        {
          "$ref": "#/definitions/UsageAggregateV1"
        }
      ]
    }
  },
  "definitions": {
    "LatencySummary": {
      "description": "Latency over the calls in an aggregate.\n\n**Percentiles are approximate** — computed over the sampled calls counted by `samples`, which is carried precisely so a consumer can see the approximation's weight instead of inferring a confidence it does not have. Tokens are exact because budgets enforce on them; latency informs (TD-0009 D3: approximate what informs, never what enforces).",
      "type": "object",
      "required": [
        "p50_ms",
        "p95_ms",
        "samples"
      ],
      "properties": {
        "p50_ms": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "p95_ms": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "samples": {
          "description": "Calls that reported a duration. Zero means the rest of this struct is meaningless.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "ttft_p50_ms": {
          "description": "Time to first token, streams only; `samples` for it may be lower than `samples` above.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "RunUsageNodeV1": {
      "description": "One node of a run's cost tree: a `(step_id, parent_id)` pair with its own spend and the rollup of itself plus every descendant.\n\n`step_id` is unique only *per parent*: the same step name under two different parents is two nodes (pinned by test). Latency is omitted — percentiles do not merge across nodes.",
      "type": "object",
      "required": [
        "own",
        "rollup",
        "step_id"
      ],
      "properties": {
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RunUsageNodeV1"
          }
        },
        "own": {
          "description": "Spend of the calls attributed directly to this step (`key` = `step_id`).",
          "allOf": [
            {
              "$ref": "#/definitions/UsageAggregateV1"
            }
          ]
        },
        "parent_id": {
          "description": "The parent step, when one was declared. A parent naming no step in this run is an orphan and this node surfaces as a root (cross-run nesting is out of scope for v1).",
          "type": [
            "string",
            "null"
          ]
        },
        "rollup": {
          "description": "`own` plus every descendant's `own` — what this step *cost*, subtree-inclusive.",
          "allOf": [
            {
              "$ref": "#/definitions/UsageAggregateV1"
            }
          ]
        },
        "step_id": {
          "description": "The step this node aggregates, or [`NONE_KEY`] for events that carried no `step_id`.",
          "type": "string"
        }
      }
    },
    "UsageAggregateV1": {
      "description": "One folded row: neutral units only, no dollars (ADR-0001).",
      "type": "object",
      "required": [
        "billable_tokens",
        "cache_creation_tokens",
        "cache_read_tokens",
        "calls",
        "key",
        "reasoning_tokens",
        "tokens_in",
        "tokens_out"
      ],
      "properties": {
        "billable_tokens": {
          "description": "The ADR-0005 D4 quantity budgets are enforced on, summed **per call**. Not derivable from the fields above: the reasoning fold is a per-call decision, so comparing summed reasoning against summed output gives a different — wrong — answer.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "cache_creation_tokens": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "cache_read_tokens": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "calls": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "key": {
          "description": "The group key — a subject/group/provider/model/virtual key/session, or one of [`TOTAL_KEY`] / [`NONE_KEY`] / [`OVERFLOW_KEY`].",
          "type": "string"
        },
        "latency": {
          "description": "Absent when no call in this row reported a duration.",
          "anyOf": [
            {
              "$ref": "#/definitions/LatencySummary"
            },
            {
              "type": "null"
            }
          ]
        },
        "reasoning_tokens": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "tokens_in": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "tokens_out": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      }
    }
  }
}
